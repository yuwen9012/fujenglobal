function showImageTC2(src) {
    const carousel = document.querySelector('#carouselExampleIndicators');
    const carouselItems = carousel.querySelectorAll('.carousel-item');
    const carouselInstance = bootstrap.Carousel.getInstance(carousel);
    const mainImage = document.getElementById('mainImage2');
    mainImage.src = src;

    carouselItems.forEach((item, index) => {
        const images = item.querySelectorAll('image');
        images.forEach(img => {
            if (img.src.includes(src)) {
                carouselInstance.to(index); // 滑动到包含目标图片的 carousel-item
            }
        });
    });
}

function showImageTC3(src) {
    const carousel = document.querySelector('#carouselExampleIndicators3');
    const carouselItems = carousel.querySelectorAll('.carousel-item');
    const carouselInstance = bootstrap.Carousel.getInstance(carousel);
    const mainImage = document.getElementById('mainImage3');
    mainImage.src = src;

    carouselItems.forEach((item, index) => {
        const images = item.querySelectorAll('image');
        images.forEach(img => {
            if (img.src.includes(src)) {
                carouselInstance.to(index); // 滑动到包含目标图片的 carousel-item
            }
        });
    });
}

function showImageTC4(src) {
    const carousel = document.querySelector('#carouselExampleIndicators4');
    const carouselItems = carousel.querySelectorAll('.carousel-item');
    const carouselInstance = bootstrap.Carousel.getInstance(carousel);
    const mainImage = document.getElementById('mainImage4');
    mainImage.src = src;

    carouselItems.forEach((item, index) => {
        const images = item.querySelectorAll('image');
        images.forEach(img => {
            if (img.src.includes(src)) {
                carouselInstance.to(index); // 滑动到包含目标图片的 carousel-item
            }
        });
    });
}

function showImageTC5(src) {
    const carousel = document.querySelector('#carouselExampleIndicators5');
    const carouselItems = carousel.querySelectorAll('.carousel-item');
    const carouselInstance = bootstrap.Carousel.getInstance(carousel);
    const mainImage = document.getElementById('mainImage5');
    mainImage.src = src;

    carouselItems.forEach((item, index) => {
        const images = item.querySelectorAll('image');
        images.forEach(img => {
            if (img.src.includes(src)) {
                carouselInstance.to(index); // 滑动到包含目标图片的 carousel-item
            }
        });
    });
}

function showImageTC6(src) {
    const carousel = document.querySelector('#carouselExampleIndicators6');
    const carouselItems = carousel.querySelectorAll('.carousel-item');
    const carouselInstance = bootstrap.Carousel.getInstance(carousel);
    const mainImage = document.getElementById('mainImage6');
    mainImage.src = src;

    carouselItems.forEach((item, index) => {
        const images = item.querySelectorAll('image');
        images.forEach(img => {
            if (img.src.includes(src)) {
                carouselInstance.to(index); // 滑动到包含目标图片的 carousel-item
            }
        });
    });
}

function showImageTC7(src) {
    const carousel = document.querySelector('#carouselExampleIndicators7');
    const carouselItems = carousel.querySelectorAll('.carousel-item');
    const carouselInstance = bootstrap.Carousel.getInstance(carousel);
    const mainImage = document.getElementById('mainImage7');
    mainImage.src = src;

    carouselItems.forEach((item, index) => {
        const images = item.querySelectorAll('image');
        images.forEach(img => {
            if (img.src.includes(src)) {
                carouselInstance.to(index); // 滑动到包含目标图片的 carousel-item
            }
        });
    });
}

function showImageTC8(src) {
    const carousel = document.querySelector('#carouselExampleIndicators8');
    const carouselItems = carousel.querySelectorAll('.carousel-item');
    const carouselInstance = bootstrap.Carousel.getInstance(carousel);
    const mainImage = document.getElementById('mainImage8');
    mainImage.src = src;

    carouselItems.forEach((item, index) => {
        const images = item.querySelectorAll('image');
        images.forEach(img => {
            if (img.src.includes(src)) {
                carouselInstance.to(index); // 滑动到包含目标图片的 carousel-item
            }
        });
    });
}

function showImageTC9(src) {
    const carousel = document.querySelector('#carouselExampleIndicators9');
    const carouselItems = carousel.querySelectorAll('.carousel-item');
    const carouselInstance = bootstrap.Carousel.getInstance(carousel);
    const mainImage = document.getElementById('mainImage9');
    mainImage.src = src;

    carouselItems.forEach((item, index) => {
        const images = item.querySelectorAll('image');
        images.forEach(img => {
            if (img.src.includes(src)) {
                carouselInstance.to(index); // 滑动到包含目标图片的 carousel-item
            }
        });
    });
}